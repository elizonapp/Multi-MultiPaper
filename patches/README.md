This directory holds the MultiPaper patch sets, one folder per Purpur version.

Each version folder contains:
  api/     - patches applied to the Purpur API
  server/  - patches applied to the Purpur server

The patch set that is applied is chosen by the "purpurVersion" property in
gradle.properties (see ./setup.sh and purpur-versions.properties).
